services:
  server:
    image: ghcr.io/se2gruppe3/saboteur:latest

    restart: unless-stopped

    ports:
      - "53207:8080"


    volumes:
      - saboteur_data:/data
      - /home/grp-3/.env:/app/.env

volumes:
  saboteur_data: